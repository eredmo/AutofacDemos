<Window x:Class="AD11.MainWindow.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AD11"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="400" Background="{Binding Background}">
    <Canvas>
        <Button Command="{Binding SetWhiteCommand}" Canvas.Left="10" Canvas.Top="10" Width="80">Set White</Button>
        <Button Command="{Binding SetYellowCommand}" Canvas.Left="10" Canvas.Top="40" Width="80">Set Yellow</Button>
        <Button Command="{Binding SetBlueCommand}" Canvas.Left="10" Canvas.Top="70" Width="80">Set Blue</Button>
        <Button Command="{Binding SethGreenCommand}" Canvas.Left="10" Canvas.Top="100" Width="80">Seth Green</Button>
    </Canvas>
</Window>
